# Pinned to match the sibling Whatsapp-Chatbot-Gov repo's working .venv
# (checked 2026-08-23) for the packages we import in-process via
# bolo/bridge/govscheme.py -- keep these in sync if that repo upgrades.
fastapi==0.128.8
uvicorn==0.39.0
pydantic==2.13.4
python-dotenv==1.2.1
httpx==0.28.1
requests==2.32.5
agora-token-builder==1.0.0
python-multipart==0.0.20
openai==2.44.0
pandas==2.3.3
numpy==2.0.2
chromadb==1.5.9
langgraph==0.6.11
langchain-core==0.3.86
langchain-openai==0.3.35

# Studio knowledge base generation (scripts/build_knowledge_base.py) only
python-docx==1.1.2

# dev/test only
pytest==8.4.2
